<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Player Details</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container my-5">
        <% if (player) { %>
            <h2 class="text-center mb-4"><%= player.fullName %></h2>
            <div class="row">
                <div class="col-md-4">
                    <h5>FULL NAME</h5>
                    <p><strong><%= player.fullName %></strong></p>
                </div>
                <div class="col-md-4">
                    <h5>BORN</h5>
                    <p><strong><%= player.birthDate.toDateString() %>, <%= player.birthPlace %></strong></p>
                </div>
                <div class="col-md-4">
                    <h5>AGE</h5>
                    <p><strong><%= player.age %></strong></p>
                </div>
            </div>
            <div class="row mt-4">
                <div class="col-md-4">
                    <h5>BATTING STYLE</h5>
                    <p><strong><%= player.battingStyle %></strong></p>
                </div>
                <div class="col-md-4">
                    <h5>BOWLING STYLE</h5>
                    <p><strong><%= player.bowlingStyle %></strong></p>
                </div>
                <div class="col-md-4">
                    <h5>PLAYING ROLE</h5>
                    <p><strong><%= player.playingRole %></strong></p>
                </div>
            </div>
        <% } else { %>
            <p class="text-center text-danger">Player details not found.</p>
        <% } %>
        <div class="text-center mt-4">
            <a href="/teams" class="btn btn-primary">Back to Teams</a>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
