<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Player Details</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container my-5">
        <% if (player) { %>
            <div class="card shadow-lg">
                <div class="card-header bg-info text-white text-center">
                    <h2>Player Details</h2>
                </div>
                <div class="card-body">
                    <h5>Name: <%= player.name %></h5>
                    <% if (player.stats) { %>
                        <p>Runs: <%= player.stats.runs %></p>
                        <p>Wickets: <%= player.stats.wickets %></p>
                        <p>Role: <%= player.role %></p>
                    <% } else { %>
                        <p>No additional player stats available.</p>
                    <% } %>
                </div>
            </div>
        <% } else { %>
            <p class="text-center text-danger">Player not found.</p>
        <% } %>
        <div class="text-center mt-4">
            <a href="/teams" class="btn btn-primary">Back to Teams</a>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
